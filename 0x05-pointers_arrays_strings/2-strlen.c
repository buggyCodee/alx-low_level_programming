#include "main.h"

/**
 * _strlen - function that returns the length of a string
 *
 * @s: string to get it's size
 *
 * Return: int that's the size of the string
 */

int _strlen(char *s)
{
	int size = 0;

	while (s[size++] != '\0')

	return (size);
}
